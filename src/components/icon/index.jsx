import './index.less';
const Icon = ({ type }) => {
    const className = `iconfont ${type || ''}`;
    return <span className={className}></span>;
};
export default Icon;
//# sourceMappingURL=index.jsx.map